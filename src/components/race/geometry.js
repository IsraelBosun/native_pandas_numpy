// Shared pixel layout constants for the race track — one place so the
// player car's vertical position and the obstacle/coin collision band always
// agree on where "the car" actually is.
export const LANE_ENTITY_SIZE = 46;
export const CAR_WIDTH = 46;
export const CAR_HEIGHT = 68;
export const BOTTOM_OFFSET = 110;
export const BASE_TRAVEL_DURATION_MS = 2600;
